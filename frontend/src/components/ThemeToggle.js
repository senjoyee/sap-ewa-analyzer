import React from 'react';

// ThemeToggle component is now empty since we're only using light theme
const ThemeToggle = () => {
  // Return null to render nothing
  return null;
};

export default ThemeToggle;
