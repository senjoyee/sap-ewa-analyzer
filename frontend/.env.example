# API Base URL
# Leave empty to use same-origin (relies on proxy in dev, or same domain in production)
# Set to full backend URL if frontend and backend are on different domains
REACT_APP_API_BASE=

# Example for different domain:
# REACT_APP_API_BASE=https://your-backend-api.azurewebsites.net
