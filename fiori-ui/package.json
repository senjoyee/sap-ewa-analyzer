{
  "name": "ewa-analyzer-fiori",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "ui5 serve --config ui5.yaml --open index.html",
    "build": "ui5 build --config ui5.yaml"
  },
  "devDependencies": {
    "@ui5/cli": "^3.3.4"
  }
}
